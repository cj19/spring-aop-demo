spring.application.name=spring-aop-demo

# Boot-time demo runner (baseline vs POC side-by-side). Off by default.
# Enable with: demo.runner.enabled=true  (or pass --demo.runner.enabled=true)
demo.runner.enabled=false
